@{
    ViewData["Title"] = "Página Inicial";
}

<div class="container text-center mt-5">
    <h1 class="mb-4" style="color: black; font-size: 2.5rem; font-weight: bold;">¿Qué quieres hacer?</h1>
    <div class="row justify-content-center">
        <div class="col-md-4 mb-4">
            <a href="@Url.Action("Index", "Agendamiento")" class="btn btn-lg btn-block action-card">Agendar Consulta</a>
        </div>
        <div class="col-md-4 mb-4">
            <a href="#" class="btn btn-lg btn-block action-card">Consultas del día</a>
        </div>
        <div class="col-md-4 mb-4">
            <a href="@Url.Action("Index", "Producto")" class="btn btn-lg btn-block action-card">Productos</a>
        </div>
    </div>
    <div class="row justify-content-center">
        <div class="col-md-4 mb-4">
            <a href="@Url.Action("Index", "Registro")" class="btn btn-lg btn-block action-card">Registros Médicos</a>
        </div>
        <div class="col-md-4 mb-4">
            <a href="@Url.Action("Index", "Informes")" class="btn btn-lg btn-block action-card">Informes</a>
        </div>
        <div class="col-md-4 mb-4">
            <a href="@Url.Action("Index", "Notification")" class="btn btn-lg btn-block action-card">Notificaciones</a>
        </div>
    </div>
</div>

<style>
    .action-card {
        display: flex;
        justify-content: center;
        align-items: center;
        background-color: #b5d6cb;
        color: black;
        font-size: 1.5rem;
        padding: 40px;
        border-radius: 15px;
        text-decoration: none;
        font-weight: bold;
        width: 100%;
        height: 120px;
    }

        .action-card:hover {
            background-color: #a0c4b8;
            color: black;
        }
</style>
